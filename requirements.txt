# Benötigte Python-Bibliotheken für projekt_autoencoder_elektromotor
# Installation (alle auf einmal):  pip install -r requirements.txt
# Empfohlen: Python 3.9 oder neuer, in einer virtuellen Umgebung (venv).

pandas>=2.0        # Einlesen und Verarbeiten der Excel-Tabellen
numpy>=1.24         # Numerische Berechnungen (Arrays, Statistik)
openpyxl>=3.1       # Lese-/Schreib-Engine für .xlsx-Dateien (von pandas benötigt)
scikit-learn>=1.3   # StandardScaler, Train/Val-Split, Bewertungsmetriken
joblib>=1.3          # Speichern/Laden des Scalers
tensorflow>=2.15    # Autoencoder-Modell (Keras, in TensorFlow enthalten)
